model Project {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
}

model ECUFile {
  id        String   @id @default(cuid())
  name      String
  size      Int
  projectId String
}